/*import assignments from "../../controllers/assignments/index.js";
import { Router } from "express";

const router = Router();

router.post("/assignment", 
    assignments.post.validator,
    assignments.post.handler
);

router.get("/assignments",
    assignments.get.validator,
    assignments.get.handler
);

router.put("/assignment/:id",
  assignments.put.validator,
  assignments.put.handler
);

router.delete("/assignment/:id",
  assignments.delete.validator,
  assignments.delete.handler
);

router.delete("/assignments",
  assignments.deleteAll.validator,
  assignments.deleteAll.handler
);

export default router;
*/