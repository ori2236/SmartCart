/*import subjects from "../../controllers/subjects/index.js";
import { Router } from "express";

const router = Router();

router.post(
  "/subject", 
  subjects.post.validator, 
  subjects.post.handler
);

router.get(
  "/subjects",
  subjects.get.validator, 
  subjects.get.handler
);

router.delete(
  "/subject/:id",
  subjects.delete.validator,
  subjects.delete.handler
);

export default router;
*/